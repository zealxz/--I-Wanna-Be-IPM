my-git-uploader/
├─ package.json
├─ electron-main.js          # Electron 主进程入口
├─ /src
│  ├─ /renderer              # 前端界面（渲染进程）
│  │   ├─ index.html
│  │   ├─ main.jsx           # 前端入口 (React/Vue皆可，这里用React当例子)
│  │   ├─ /components        # 各个界面的组件
│  │   │    ├─ ScreenSelectRole.jsx      # 界面1：我是高手 / 我是小白
│  │   │    ├─ ScreenLogin.jsx           # 界面2：登录/认证信息输入
│  │   │    ├─ ScreenHelp.jsx            # 界面3：获取密钥的教学引导
│  │   │    ├─ ScreenUpload.jsx          # 界面4：拖拽/选择文件夹并上传
│  │   │    └─ CommonModal.jsx           # 通用弹窗（缺少README等时提醒）
│  │   ├─ /assets            # 界面需要用到的图示、教程截图
│  │   ├─ /styles
│  │   │    └─ app.css
│  │   └─ /state
│  │        └─ uiState.js    # 前端状态，比如当前在哪个界面、用户输入的表单
│  ├─ /preload               # Electron preload 脚本，负责前后端安全通信
│  │   └─ preload.js
│  └─ /backend               # Node侧逻辑（主进程/子进程可调用）
│      ├─ gitOps.js          # Git相关操作（init/add/commit/push）
│      ├─ repoValidate.js    # 校验本地目录里有没有.gitignore和README.md
│      ├─ configStore.js     # 保存/读取用户配置(用户名、邮箱、密钥路径等)
│      └─ pathUtils.js       # 路径处理等工具
└─ /resources
├─ templates
│    ├─ default_README.md # 给小白的README默认模板
│    └─ default_gitignore # 给小白的.gitignore默认模板
└─ icons
└─ app-icon.png      # 应用图标
